import 'package:flutter/material.dart';

class FlagDetail extends StatelessWidget {
  const FlagDetail({Key key, @required this.flagId}) : super(key: key);

  final String flagId;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
